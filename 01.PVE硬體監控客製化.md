# PVE 硬體監控客製化

> **正式版：disk_monitor.sh v1.3.6-Pro**  
> 更新日期：2026-09-11  
> 支援：Proxmox VE 9.x / Debian 13 Trixie

本專案在 Proxmox VE Node Summary 整合 CPU、CPU 溫度、網卡溫度、NVMe、SATA/SAS、MegaRAID Physical Disk 與 SMART 等硬體資訊。

核心設計是：**硬體資料由背景 `collect` 採集並寫入 runtime，PVE API request 只讀取整理後的 JSON，不在 Web request 期間直接執行 `smartctl`、`sensors`、`turbostat`。**

---

## 1. 目錄與檔案

```text
src/pve/
├── disk_monitor.sh
```

對應實機畫面：

```text
img/p01/
```

---

## 2. disk_monitor.sh v1.3.6-Pro

```bash
VERSION="1.3.6-Pro"
UPDATED="2026-09-11"
```

主要工作分成三層：

```text
┌─────────────────────────────┐
│ disk_monitor.sh             │
├─────────────────────────────┤
│ ① 硬體資料採集              │
│   smartctl / sensors /      │
│   lsblk / turbostat / pvesh │
├─────────────────────────────┤
│ ② Runtime JSON              │
│   /run/disk_monitor_runtime │
│   /run/disk_monitor.json    │
├─────────────────────────────┤
│ ③ PVE UI / API Hook         │
│   Nodes.pm                  │
│   pvemanagerlib.js          │
│   proxmoxlib.js             │
└─────────────────────────────┘
```

### 2.1 CPU

採集 CPU scaling governor、CPU minimum / maximum frequency、CPU 即時 MHz、CPU package `PkgWatt`、CPU / 多插槽溫度與網卡感測器溫度。

### 2.2 NVMe

整合 NVMe SMART 與健康資訊，包括型號、溫度、健康度、Power On Hours、Power Cycle、Read / Write TB 與 SMART 狀態。

### 2.3 SATA / SAS

透過 `lsblk` 的 `TRAN` 與 `ROTA` 判斷裝置類型，區分 SATA/SAS SSD 與 HDD。

### 2.4 MegaRAID Physical Disk

MegaRAID 使用 `smartctl` 搭配 MegaRAID device type 逐一掃描 Physical Disk，取得型號、Serial、WWN、溫度與 SMART 狀態。

另外建立 RAID Map，優先使用 Serial，再以 WWN 對應 `/dev/sdX` 與 RAID Physical Disk，避免 RAID 磁碟被重複列入一般 SATA/SAS 清單。

### 2.5 SMART 狀態

程式會依 SMART 回傳結果整理成：

```text
OK
FAIL
UNKNOWN
```

不會把無法判定的資料錯誤標示成正常。

---

## 3. 背景採集架構

```text
/run/disk_monitor_runtime/
/run/disk_monitor.json
```

Cron：

```text
/etc/cron.d/disk_monitor
```

正式部署後由 cron 每分鐘執行：

```bash
/root/disk_monitor.sh collect
```

資料採集完成後，PVE UI 直接讀取已整理好的 runtime JSON。

重點是：**開啟 Node Summary 時不需要重新掃描全部硬碟 SMART，也不需要在 API request 裡執行 sensors / turbostat。**

---

## 4. PVE 官方檔案與 Hook

```text
/usr/share/perl5/PVE/API2/Nodes.pm
/usr/share/pve-manager/js/pvemanagerlib.js
/usr/share/javascript/proxmox-widget-toolkit/proxmoxlib.js
```

CPU 與磁碟資訊分別使用精準 Hook 錨點插入；`Nodes.pm` 只讀取背景採集結果，不在 request 期間直接執行完整硬體掃描。

---

## 5. Node Summary 自適應高度

大量硬碟時，固定高度容易造成內容被截斷，因此 Node Summary 採用自適應內容高度，讓磁碟數量增加時 UI 能跟著內容延伸。

---

## 6. 安裝與操作

### 6.1 取得正式版

```bash
curl -fsSL https://raw.githubusercontent.com/sungshu/Pve-Toolkits/main/src/pve/disk_monitor.sh -o /root/disk_monitor.sh
chmod +x /root/disk_monitor.sh
```

### 6.2 All-in-One 安裝

```bash
/root/disk_monitor.sh
```

### 6.3 手動採集

```bash
/root/disk_monitor.sh collect
```

### 6.4 重新套用 UI

```bash
/root/disk_monitor.sh remod
```

### 6.5 還原官方 PVE

```bash
/root/disk_monitor.sh restore
```

---

## 7. 官方檔案備份

```text
/var/lib/disk_monitor/<PVE版本>/
```

包含：

```text
Nodes.pm
pvemanagerlib.js
proxmoxlib.js
```

程式會檢查既有 backup 是否已含 `disk_monitor` 注入，避免把客製化檔案誤當成官方原始檔案。

---

## 8. v1.3.6-Pro

目前正式版為 `disk_monitor.sh v1.3.6-Pro`，維持背景硬體採集、runtime JSON、RAID Map、SMART 與 PVE Node Summary 客製化架構。

版本資訊：

```text
Version:  1.3.6-Pro
Updated:  2026-09-11
Support:  Proxmox VE 9.x / Debian 13 Trixie
```

---

## 9. v1.3.6-Pro 實際部署畫面

![PVE 硬體監控畫面 01](img/p01/2026-09-15%20124813.png)

![PVE 硬體監控畫面 02](img/p01/2026-09-15%20124916.png)

![PVE 硬體監控畫面 03](img/p01/2026-09-15%20124957.png)

![PVE 硬體監控畫面 04](img/p01/2026-09-15%20125026.png)

---

## 10. 驗證方式

```bash
jq empty /run/disk_monitor.json
pvesh get /nodes/localhost/status --output-format json | jq '.cpuFreq,.thermalstate'
cat /etc/cron.d/disk_monitor
ls -lah /run/disk_monitor_runtime/
ls -lah /run/disk_monitor.json
```

更新 UI 後建議執行瀏覽器：

```text
Ctrl + F5
```

---

## 11. PVE 升級後處理

PVE 更新可能覆蓋官方檔案，因此升級後應重新確認：

```text
Nodes.pm
pvemanagerlib.js
proxmoxlib.js
```

建議流程：

```text
PVE 升級
  ↓
確認官方檔案版本
  ↓
確認 disk_monitor backup
  ↓
重新執行 disk_monitor.sh
  ↓
collect
  ↓
確認 API JSON
  ↓
Ctrl + F5
  ↓
確認 Node Summary
```

不要直接把舊版修改後的 PVE 官方檔案覆蓋到新版 PVE 上。

---

## 12. 結尾

`disk_monitor.sh v1.3.6-Pro` 的目的不是單純把幾個 SMART 數值塞進 PVE Web UI，而是把**硬體資料採集、背景快取、RAID 對應、PVE API、前端 Hook、官方檔案備份與還原**整合成一套可以持續維護的監控方案。

實際部署畫面已完成驗證；後續如果 PVE 版本升級，只需要重新確認官方檔案與 Hook 錨點，再依本文件流程重新套用即可。
