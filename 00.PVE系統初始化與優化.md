# PVE Toolkit｜系統初始化與優化

**Proxmox VE Infrastructure Toolkit**：PVE 9（Debian 13 Trixie）主機初始化與優化流程，以及 Node Summary 硬體監控整合。

對應主腳本：[`pve_init.sh`](src/pve/pve_init.sh) **v2.1.9**

## 1. 初始化內容

`pve_init.sh` 會依序處理：

1. **APT Repository**：備份現有設定，清理 Enterprise source，建立 Debian 13、PVE no-subscription source；使用 TWDS 台灣鏡像作為 Debian 來源。
2. **時區與時間同步**：設定 `Asia/Taipei`，使用 Chrony 與台灣公開 NTP；可指定內部 NTP。
3. **PVE Subscription Nag Hook**：設定 PVE Web UI 訂閱提示修補機制。
4. **必要套件**：安裝硬體監控與系統管理所需套件。
5. **Datacenter Tag**：設定 `tag-style: shape=full,ordering=alphabetical`。
6. **硬體監控**：自動下載並部署 `disk_monitor.sh v1.3.6-Pro` 至 `/root/disk_monitor.sh`。

## 2. 一鍵初始化

預設不執行完整 `apt full-upgrade`。

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/sungshu/Pve-Toolkits/main/src/pve/pve_init.sh)
```

版本相同時，會顯示 **30 秒倒數**；按 **Enter** 可立即執行 `disk_monitor.sh`，不按則倒數結束後自動執行。

## 3. 完整升級

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/sungshu/Pve-Toolkits/main/src/pve/pve_init.sh) --upgrade
```

## 4. Ceph Repository

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/sungshu/Pve-Toolkits/main/src/pve/pve_init.sh) --ceph
bash <(curl -fsSL https://raw.githubusercontent.com/sungshu/Pve-Toolkits/main/src/pve/pve_init.sh) --ceph --upgrade
```

Ceph 與完整升級一起執行時，`--ceph` 僅加入 Ceph Squid no-subscription repository，不會建立 Ceph Cluster 或 OSD。

## 5. 內部 NTP

```bash
INTERNAL_NTP=192.168.0.100 bash <(curl -fsSL https://raw.githubusercontent.com/sungshu/Pve-Toolkits/main/src/pve/pve_init.sh)
```

`192.168.0.100` 僅為範例，請替換為實際 NTP 位址。

## 6. 重新套用與還原硬體監控 UI

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/sungshu/Pve-Toolkits/main/src/pve/pve_init.sh) remod
bash <(curl -fsSL https://raw.githubusercontent.com/sungshu/Pve-Toolkits/main/src/pve/pve_init.sh) restore
```

## 7. 驗證

```bash
sensors
smartctl --scan-open
nvme list
systemctl status chrony --no-pager
```

硬體監控完整架構、安裝流程與實機畫面請參閱：

👉 [01.PVE硬體監控客製化](01.PVE硬體監控客製化.md)

## 8. 2026-09-15 實際安裝與更新畫面

![PVE Toolkit 安裝畫面 01](img/p00/2026-09-15%20110653.png)

![PVE Toolkit 安裝畫面 02](img/p00/2026-09-15%20122033.png)

![PVE Toolkit 安裝畫面 03](img/p00/2026-09-15%20122040.png)

![PVE Toolkit 安裝畫面 04](img/p00/2026-09-15%20122153.png)

![PVE Toolkit 安裝畫面 05](img/p00/2026-09-15%20122232.png)

![PVE Toolkit 安裝畫面 06](img/p00/2026-09-15%20122332.png)

![PVE Toolkit 更新畫面](img/p00/2026-09-15%20122359.png)

## 9. 注意事項

- 執行前會備份現有 APT 設定至 `/root/apt-sources-backup-日期時間/`。
- `--upgrade` 可能更新 PVE 核心；完成後依實際環境安排重開機。
- `--ceph` 僅加入 Ceph Squid no-subscription repository，不會建立 Ceph Cluster 或 OSD。
- 本工具不會自動處理 VM、CT、Storage、Network、Firewall、Ceph Cluster／OSD 或 VMware 遷移。
- PVE 套件版本更新後，客製化 UI 的插入點可能變更；正式環境建議先於測試節點驗證。
